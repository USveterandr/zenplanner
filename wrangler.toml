name = "zen-planner"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".vercel/output"

# Note: Build command (npm run build:cf) and NODE_VERSION (20) 
# must be configured in the Cloudflare Pages Dashboard.
